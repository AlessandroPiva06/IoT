# ============================================================
#  SmartStation — Variabili d'ambiente
#  Copia questo file in .env e compila i campi
# ============================================================

# --- Server ---
PORT=3000
SERVER_HOST=localhost          # oppure l'IP del PC sulla LAN

# --- MQTT (Mosquitto) ---
MQTT_HOST=localhost            # IP del broker Mosquitto
MQTT_PORT=1883

# --- Email (Gmail App Password consigliata) ---
# Per Gmail: Impostazioni → Account → Sicurezza → Password per le app
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=alessandro.piva@fermi.mo.it
EMAIL_PASS=wsoc pptm ccqr ckba  # App Password (NON la password Google)
EMAIL_TO=u5374286119@id.gle
